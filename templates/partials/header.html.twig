<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('app_home') }}">
        <img src="{{ asset('img/logo2.png') }}" width="60" height="60" alt="logo">
    </a>
        <a class="navbar-brand" href="#">Villes</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link active" href="{{ path('app_login') }}">Sites</a>
                <a class="nav-item nav-link" href="{{ path('app_home') }}">Accueil</a>
                <a class="nav-item nav-link" href="{{ path('app_register') }}">Mon profil</a>
                <a class="nav-item nav-link " href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
        </div>
    </div>
</nav>